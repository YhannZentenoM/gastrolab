---
interface Props {
    title: string;
}
const { title } = Astro.props;
---
<section class="w-full bg-[url('/recursos/titlebar-bg.jpg')] bg-cover flex justify-center items-center min-h-[250px] md:min-h-[400px]">
    <h1 class="text-white text-2xl md:text-4xl font-bold">{title}</h1>
</section>